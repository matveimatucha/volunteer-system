name: Site uptime check

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check /health
        run: |
          body=$(curl -fsS --max-time 30 https://volonter-msu.ru/health)
          echo "$body"
          echo "$body" | grep -q '"ok":true'

      - name: Check main page
        run: |
          code=$(curl -fsS --max-time 30 -o /dev/null -w "%{http_code}" https://volonter-msu.ru/)
          echo "HTTP $code"
          test "$code" = "200"

      - name: Check registration page
        run: |
          code=$(curl -fsS --max-time 30 -o /dev/null -w "%{http_code}" https://volonter-msu.ru/register.html)
          echo "HTTP $code"
          test "$code" = "200"
